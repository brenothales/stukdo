<%= simple_form_for(@subtask) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :complete %>
    <%= f.association :task %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>


  <div class="row ">
<h1>
  Visão Geral 
  <small>Acompanhamento de Tarefas</small>
</h1>
  </div>
    <div class="row searchable-container">
      <% @users.each do |u| %>
        <div class="col-sm-3">
            <div class="card items">
                <canvas class="header-bg" width="250" height="70" id="header-blur"></canvas>
                <div class="avatar">
                    <img src="" alt="" />
                    <%= image_tag(u.avatar.url(:thumb), class:"src-image" )%>
                </div>
                <div class="content">
                    <p> <% unless u.nome.empty? -%>
                           <%= u.nome %> <%= u.sobrenome %>
                        <% else %>
                           <%= u.email %>
                        <% end %> <br>
                        <%= u.setor if u.setor.present? %> <br>
                        <%= u.funcacao if u.funcacao.present? %> 
                        <%= u.celular if u.celular.present? %> 
                    </p>
                    <p>
                      <button type="button" class="btn btn-default"><%= link_to "Tarefas", u %>  <%= u.tasks.public_tasks.count %>
                      </button>
                    </p>
                </div>
            </div>
        </div>
      <% end -%>
    </div>





